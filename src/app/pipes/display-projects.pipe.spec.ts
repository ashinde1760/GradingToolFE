import { DisplayProjectsPipe } from './display-projects.pipe';

describe('DisplayProjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayProjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
