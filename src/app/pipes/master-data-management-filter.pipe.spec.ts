import { MasterDataManagementFilterPipe } from './master-data-management-filter.pipe';

describe('MasterDataManagementFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MasterDataManagementFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
